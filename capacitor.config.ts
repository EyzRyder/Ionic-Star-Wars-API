import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'star_wars_api',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
